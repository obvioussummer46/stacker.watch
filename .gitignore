# Xcode / SwiftPM build products
xcuserdata/
DerivedData/
.build/
.swiftpm/
Package.resolved
*.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/

# Local signing (Apple Team ID). See StackerWatch/Signing.xcconfig.
Local.xcconfig

# Xcode distribution output. Signed .ipa bundles carry embedded.mobileprovision,
# which contains the team id, the signing certificate and, for ad-hoc profiles,
# tester device UDIDs. Xcode's Organizer drops these wherever you point it.
*.ipa
*.xcarchive/
/StackerWatch */

.DS_Store
